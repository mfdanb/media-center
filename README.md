# media-center

tl;dr: my multimedia server. for a high level walkthrough, read [this](WALKTHROUGH.md).


to run:

```
docker compose up -d
```

to update images:

```
docker compose pull
docker compose up -d
```